<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Admin Panel</title>
</head>
<body>
  <nav class="bg-gray-800">
    <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
        <div class="relative flex h-16 items-center justify-between">
            <div class="flex flex-1 items-center justify-center sm:items-stretch sm:justify-start">
                <div class="hidden sm:ml-6 sm:block">
                    <div class="flex space-x-4">
                      <a href="/logout" class="text-gray-300 hover:bg-red-500 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Logout</a>
                      <a href="/shop" class="text-gray-300 hover:bg-gray-700 hover:text-white rounded-md px-3 py-2 text-sm font-medium">Products</a>
                    </div>
                </div>
            </div>
            <div class="absolute inset-y-0 right-0 flex items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0">
              <a href="/add-product" class=" rounded-md px-3 py-2 text-sm font-medium text-white bg-green-500 rounded-md" aria-current="page">Add Product</a>
            </div>
        </div>
    </div>
  </nav>
  <div class="bg-white">
    <div class="mx-auto max-w-2xl px-4 py-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
      <h2 class="sr-only">Products</h2>
      <div class="grid grid-cols-1 gap-x-6 gap-y-10 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 xl:gap-x-8">
        <% products.forEach(product => { %>
        <div class="group">
          <div class="aspect-h-1 aspect-w-1 w-full overflow-hidden rounded-lg bg-gray-200 xl:aspect-h-8 xl:aspect-w-7">
            <img src="<%= product.image %>" alt="<%= product.title %>" class="w-full h-72 object-cover object-center group-hover:opacity-75">
          </div>
          <div class=" flex items-center justify-around">
            <h3 class="mt-4 text-sm text-gray-700"><%= product.title %></h3>
            <p class="mt-1 text-lg font-medium text-gray-700"><%= product.price %></p>
          </div>
          <div class=" flex items-center justify-around mt-6">
            <a href="/edit-product/<%= product.id %>" class="bg-blue-500 text-white font-bold py-2 px-8 rounded">Edit</a>
            <form action="/delete-product/<%= product.id %>" method="post" class="bg-red-500  text-white font-bold py-2 px-6 rounded" >
              <button type="submit">Delete</button>
            </form>
          </div>
        </div>
        <% }) %>
      </div>
    </div>
  </div>
</body>
</html>
